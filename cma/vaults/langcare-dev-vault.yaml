# CMA Vault — LangCare Dev
# API: POST /v1/vaults | Beta: managed-agents-2026-04-01

display_name: LangCare Dev
credential:
  mcp_server_url: https://langcare-mcp-dev.fly.dev/mcp
  auth:
    type: bearer
    # Set via LANGCARE_DEV_API_KEY env var in deploy script
metadata:
  tier: development
  owner: langcare-team
