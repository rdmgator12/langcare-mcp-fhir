#!/usr/bin/env python3
"""Git pre-commit hook — thin wrapper. All logic lives in phi_hook.py."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from phi_hook import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
